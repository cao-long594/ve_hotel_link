# VE Hotel Link

酒店房源连接项目，将上游的标准酒店业务模型适配为各供应商的酒店静态、可订查询和订单接口。

## 易旅分销 Open API

**易旅分销 V2**:
易旅供应商的新版 GlobalStay Open API 协议；该协议覆盖酒店静态、可订查询、验价和订单全链路。
_Avoid_: 易旅新接口、易旅新版

**接口版本开关**:
供应商配置字段 `apiVersion`，值为 `v2` 时整套易旅 Open API 调用新版；空值或其他值调用旧版。
_Avoid_: 单接口开关、按接口版本降级

**客户编码**:
供应商配置中的 `customerCode`，用于标识调用易旅 Open API 的客户身份，不随酒店、订单或入住人变化。
_Avoid_: 企业编码、订单客户号

**报价**:
供应商对一个房型在给定入住条件下返回的可预订产品，包含报价编码、总价、每日价格、币种、餐食、取消政策和到店付税费。
_Avoid_: 产品列表、房价

**采购价**:
易旅 V2 报价中的 `totalPrice` 与 `dailyPriceList.price`，是供应商返回的采购金额；报价、验价和下单必须使用同一组金额。
_Avoid_: 销售价、展示价

**产品可订校验**:
对已选酒店、房型和报价在相同入住条件下进行的单产品重新确认；成功后返回校验后的房型和报价。
_Avoid_: 验库存、价格查询

**取消政策**:
易旅 V2 报价中的 `cancelPolicies`，每个节点以 `from` 表示取消费用生效时间、以 `amount` 表示从该时间起的取消扣款金额；空数组表示不可取消。
_Avoid_: 退款规则、退订说明

**入住人房间**:
按房间组织的入住人数与儿童年龄信息，房间序号从 1 开始，并与房间数一一对应。
_Avoid_: 客人列表、住客数组
